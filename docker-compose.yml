version: '3'
services:
  jekyll:
    build: .
    image: yubbit/hydejack:latest
    command: jekyll serve --incremental --force_polling  --drafts --host 0.0.0.0
    volumes:
    - .:/srv/jekyll:cached
    - ./vendor/bundle:/usr/local/bundle:cached
    ports:
    - 4000:4000
